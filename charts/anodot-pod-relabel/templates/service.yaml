apiVersion: v1
kind: Service
metadata:
  name: {{ template "template_name" . }}
spec:
  selector:
    app: {{ template "template_name" . }}
    release: {{ .Release.Name }}
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080